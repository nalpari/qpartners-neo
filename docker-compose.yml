services:
  app:
    build: .
    container_name: qpartners-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      DB_USER: development
      DB_PASSWORD: "UtCxBswla1g9beoV+dwe"
      DB_NAME: development
      DATABASE_URL: "mysql://development:UtCxBswla1g9beoV+dwe@db:3306/development"
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mariadb:11
    container_name: qpartners-db
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: development
      MYSQL_USER: development
      MYSQL_PASSWORD: "UtCxBswla1g9beoV+dwe"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  db_data:
